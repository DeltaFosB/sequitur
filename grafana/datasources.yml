apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000
      httpHeaderName1: "X-Scope-OrgID"
      httpHeaderValue1: "fake"
